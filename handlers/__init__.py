"""Handlers layer (Part II.3).

Step 2 populated this with the chain-of-responsibility Dispatcher
(dispatcher.py), the Handler protocol (base.py), and the navigation-only
handlers for /start, Home, Back, and stale-session recovery
(navigation.py). Per-screen handlers for scanning, results, trading,
settings, etc. are added starting Step 3.
"""
